server:
  port: 9000

# https://docs.spring.io/spring-boot/docs/current/reference/html/production-ready-endpoints.html#production-ready-application-info
# http://localhost:9292/info
info:
  app:
    name: config server
    description: configuration server.
    version: 1.0.0

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/enlightendev/spring-cloud-config