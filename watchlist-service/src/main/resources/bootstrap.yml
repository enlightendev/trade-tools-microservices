# bootstrap properties loaded early in application start

spring:
  cloud:
    config:
      uri: http://localhost:9000

#application.name will be used to lookup the appropriate configuration via config server
#there should be a repo with this name
  application:
    name: watchlist-service